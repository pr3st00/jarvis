pcm.!default {
  type hw
   playback.pcm {
     type hw
     slave.pcm "hw:0,1"
   }
   capture.pcm {
     type plug
     slave.pcm "hw:1,0"
   }
}

ctl.!default {
    type hw
    card 0
}


pcm.!default {
	type hw
	card 0
}

ctl.!default {
	type hw
	card 0
}
